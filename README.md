Simple NestJS API for searching and fetching data on PS1 retro games.


## Getting Started

docker-compose file is included. A simple `docker-compose up` should be enough to start both postgres DB and api instance.